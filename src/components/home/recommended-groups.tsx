import Link from "next/link";
import type { PublicGroupCard } from "@/modules/groups/contracts";

export function RecommendedGroups({ groups }: { groups: PublicGroupCard[] }) {
  return <section aria-labelledby="recommended-title" className="recommended-groups"><div className="flex items-end justify-between gap-4"><div><p className="text-sm font-bold uppercase tracking-[0.14em] text-[var(--accent-strong)]">Keep exploring</p><h2 className="font-display mt-2 text-3xl font-black tracking-[-0.06em]" id="recommended-title">Groups you might like</h2></div><Link className="text-sm font-extrabold text-[var(--accent-strong)] underline decoration-2 underline-offset-4" href="/discover">See all groups</Link></div><div className="mt-6 space-y-3">{groups.slice(0, 3).map((group) => <article className="flex items-center gap-4 rounded-2xl border border-dashed border-[var(--line-strong)] bg-[var(--surface-muted)] p-4 transition-transform hover:-translate-y-0.5" key={group.slug}><div className="flex size-11 shrink-0 items-center justify-center rounded-full bg-[var(--surface)] text-center text-[0.65rem] font-black uppercase leading-tight text-[var(--accent-strong)]">{group.sport.slice(0, 3)}</div><div className="min-w-0 flex-1"><span className="text-xs font-bold uppercase tracking-[0.1em] text-[var(--muted)]">{group.sport}</span><h3 className="font-display mt-1 truncate text-xl font-black tracking-[-0.05em]">{group.name}</h3><p className="mt-1 truncate text-sm text-[var(--muted)]">{group.location}</p></div><Link aria-label={`View ${group.name}`} className="shrink-0 text-sm font-extrabold text-[var(--accent-strong)]" href={`/groups/${group.slug}`}>View<span aria-hidden="true" className="ml-1">→</span></Link></article>)}</div></section>;
}
